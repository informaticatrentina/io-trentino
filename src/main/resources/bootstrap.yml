spring:
  application:
    name: ioApp
  profiles: dev, devcom, test
  cloud:
    config:
      #uri:


      uri: ${CONFIG_URI}
      env: default
      label: master
      password: mirko
      username: mirko
